{
  "name": "roblox-suite-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/og-image.mjs && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "check:content": "node scripts/validate-manifest.mjs && node scripts/validate-content.mjs && node scripts/check-deprecated-apis.mjs && node ../scripts/check-hub-refs.mjs && node ../scripts/generate-catalog-artifacts.mjs --check && node ../evals/validate.mjs",
    "check:dist": "node scripts/check-built-site.mjs",
    "test": "npm run check:content && npm run check && npm run build && npm run check:dist",
    "og:image": "node scripts/og-image.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource/spline-sans-mono": "^5.2.8",
    "@tailwindcss/vite": "^4.3.2",
    "astro": "^7.0.7",
    "gray-matter": "^4.0.3",
    "tailwindcss": "^4.3.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "typescript": "^6.0.3"
  },
  "overrides": {
    "esbuild": ">=0.28.1"
  }
}
